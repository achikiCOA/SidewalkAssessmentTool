(function () {
  "use strict";

  const App = window.App = window.App || {};

  App.recorderSync = App.recorderSync || {};

  App.recorderSync.sentForProcessingMessage = "Recording saved locally and sent to the backend for processing. Check the Recorder Sync Log for ArcGIS results.";
})();
